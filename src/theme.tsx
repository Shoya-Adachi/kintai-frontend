import { createTheme } from "@mui/material";

export const kintaitheme = createTheme({
    palette: {
        primary: {
          main: "#000000",
        },
        success: {
          main: "#4caf50"
        }
    }
})
